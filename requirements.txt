# Deliberately small. At this data scale a deep-learning dependency is a smell.
numpy
scipy
pandas
scikit-learn
matplotlib
requests
python-dotenv

# optional, only if the profiler moves off TF-IDF onto a sentence encoder.
# M0 uses TF-IDF deliberately (docs/06): no model download, fully deterministic.
# Uncommenting this pulls torch, which is most of CI's install time, so leave it
# commented until something actually imports it.
# sentence-transformers

# optional, only if the Bayesian/hierarchical prior work (D-007 option C) starts
# pymc
# arviz
